#include "stm32f10x.h"
#include "MyTimer.h"
#include "Driver_GPIO.h"

void commande(int recu){
	/*int Rapport;	
	
	//recu 
	Rapport = (recu)
	

	 if (Rapport < 0) {
        MyGPIO_Set(GPIOA, 6);
		 Rapport = (-1) * Rapport ; 
    } else {
        MyGPIO_Reset(GPIOA, 6);
    }					
    MyTimer_SetDutyCycle(TIM3, 1, Rapport);*/
}
